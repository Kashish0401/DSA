package Tree.BinarySearchTree;

public class comments {
    /*public Node delete(Node tempRoot,Node p, int e){
        if(tempRoot!=null){
            if(e==tempRoot.element){
                // Delete a leaf node
                if(tempRoot.left==null && tempRoot.right==null){
                    if(e<p.element){
                        p.left=null;
                    }
                    else{
                        p.right=null;
                    }
                    return p;
                }
            }
            else if(e<tempRoot.element) {
                tempRoot = tempRoot.left;
                if (tempRoot.element != e)
                    p = p.left;
                delete(tempRoot, p, e);
                }
            else if(e>tempRoot.element) {
                tempRoot = tempRoot.right;
                if (tempRoot.element != e)
                    p = p.right;
                delete(tempRoot, p, e);
            }
        }
        return p;
    }

    public Node delete(Node tempRoot, int element){
        if(tempRoot==null)
            return tempRoot;

        else if(element< tempRoot.element){
            tempRoot.left=delete(tempRoot.left,element);
        }
        else if(element> tempRoot.element){
            tempRoot.right=delete(tempRoot.right,element);
        }
        else{
            if(tempRoot.left==null)
        }
    }*/
}
